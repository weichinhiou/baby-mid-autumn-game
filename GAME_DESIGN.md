# 寶寶過中秋｜遊戲設計與實作規則

**文件日期：** 2026-09-25  
**版本基準：** GitHub 專案根目錄的 `index.html` 與同目錄圖片資產（Sites 原始專案對應 `mooncake-game/dist/index.html`）  
**遊戲網址：** https://mooncake-baby-adventure.weijinxio.chatgpt.site/

> 本文件記錄目前程式實際實作的玩法，數值若有調整，以更新後的程式與文件為準。此遊戲為單人、單回合、純前端的中秋節接物遊戲。

## 1. 遊戲目標與流程

玩家操控戴兔耳帽、以爬行姿勢呈現的像素風 Q 版寶寶，在倒數時間內接住從天空落下的物件，累積分數並盡量延長遊戲。接到有害物件會扣分、扣秒或暫時無法移動。時間歸零時立即結算；沒有通關門檻，也沒有主動提前結束按鈕。

1. 開始畫面顯示「寶寶過中秋！」、縮小的寶寶圖示、玩法說明與六種物件效果。
2. 按「開始吃大餐」後重設遊戲並開始倒數 30 秒。
3. 玩家直接拖動寶寶接物，物件持續生成、下降；物件被接到時立刻套用效果。
4. 實際遊玩時間達到 30 秒後，速度提升一階；往後每滿 10 秒再升一階。
5. 剩餘時間為零時顯示「好飽喔~中秋節快樂!!」、亮色大字的「你的得分」與「再玩一次」。

## 2. 計分、時間與物件

每次生成物件時，依下表權重隨機選一種，總權重為 100；每次抽取彼此獨立。所有物件都顯示獨立像素圖、效果文字，接到時播放各自不同的音效，並顯示向上浮動的效果提示。

| 物件 | 出現權重 | 接到的即時效果 | 遊戲內效果標示 | 圖片檔 |
| --- | ---: | --- | --- | --- |
| 月餅 | 44% | 分數 +1 | `+1分` | `mooncake-icon-fast.webp` |
| 炸彈 | 15% | 分數 −2 | `−2分` | `bomb-icon-fast.webp` |
| 熟烤肉串 | 12% | 剩餘時間 +3 秒 | `+3秒` | `skewer-cooked-fast.webp` |
| 焦烤串 | 10% | 剩餘時間 −5 秒，最低為 0 | `−5秒` | `skewer-burnt-fast.webp` |
| 未熟烤串 | 10% | 寶寶無法移動 3 秒 | `卡3秒` | `skewer-raw-fast.webp` |
| 柚子 | 9% | 分數 +5 | `+5分` | `pomelo-fast.webp` |

**邊界規則：** 分數可以是負數，沒有最低 0 分限制。接到焦烤串而將剩餘時間扣到 0，遊戲會在本幀結束時結算。未熟烤串不扣分也不扣秒；再次接到未熟烤串，卡住時間重新設為 3 秒，而非累加。卡住期間寶寶無法移動，物件仍然落下，也仍可碰撞和觸發效果。漏接任何物件不扣分、不扣秒。

## 3. 時間與難度

| 項目 | 目前規則 |
| --- | --- |
| 初始剩餘時間 | 30 秒 |
| 計時起點 | 按下開始按鈕後的遊戲畫面影格 |
| 顯示方式 | 剩餘時間向上取整為整數秒；結束顯示 0 秒 |
| 延長／縮短 | 熟烤串 +3 秒；焦烤串 −5 秒且不低於 0 |
| 加速依據 | 從**開始遊戲起累積的實際遊玩時間**，不看剩餘倒數；加秒不會重置加速進度 |
| 難度階段 | 0–未滿 30 秒為 Lv.1；30–未滿 40 秒為 Lv.2；40–未滿 50 秒為 Lv.3；之後每 10 秒升一階 |
| 下降速度 | 基礎速度 × `[1 + 0.16 × (等級 − 1)]`；升級會立即影響畫面上已存在的物件 |
| 生成節奏 | 開始約 0.4 秒後生成第一個；其後每次生成後，下一次間隔隨機為 0.45–0.70 秒 |

基礎下降速度依畫面方向不同：橫式每秒約 205–280 個虛擬畫布單位，直式每秒約 305–400 個單位；每個物件生成時各自抽取固定基礎速度。物件下降時有輕微左右擺動；超出畫面下方後移除。倒數與卡住時間都透過動畫影格更新，每一影格的時間差上限為 0.05 秒，因此瀏覽器切到背景、動畫暫停或裝置嚴重掉幀時，遊戲時間不一定等同牆上時鐘經過的時間。

## 4. 操作與碰撞

- **手機：** 以手指按住畫面上的寶寶並水平拖動。
- **電腦：** 以滑鼠按住寶寶並水平拖動。程式也保留 `←`／`→` 或 `A`／`D` 鍵盤備用操作，畫面不顯示左右方向按鈕。
- 拖曳必須從寶寶附近開始：與寶寶中心的水平距離不超過 110、垂直距離不超過 100 個虛擬單位。手指或滑鼠在其他區域直接滑動，不會瞬間移動寶寶。
- 拖曳保留按下時的水平相對位置，寶寶平滑跟隨；抬起、操作取消或視窗失焦會停止對應的移動。寶寶中心受左右邊界限制，距畫布兩側至少 85 個單位。
- 寶寶中心高度：直式為畫布高度減 95，橫式為畫布高度減 70。落物中心進入寶寶中心上方 88 到下方 30 的垂直範圍，且水平距離小於 70 個單位，視為接到。
- 卡住時不接受新的拖曳，鍵盤移動也停止。卡住後需重新按住寶寶才會開始新一段拖曳。

## 5. 畫面、文字與視覺

**首頁與遊玩畫面：** 頂部標題為「🥮 寶寶過中秋」，顯示得分、剩餘秒數與音效開關。開始卡片標題為「寶寶過中秋！」，下方有小尺寸寶寶圖。介紹文字為「拖動爬行寶寶，接住落下的好吃東東。」並換行說明時間結算與加速。物件圖示與效果一併列於開始卡片。底部操作提示分兩行：「手機：以手指拖動畫面上的寶寶。」及「電腦：以滑鼠拖動畫面上的寶寶。」

**結算畫面：** 顯示「好飽喔~中秋節快樂!!」、大字亮黃色得分與「再玩一次」。不逐項列出每種物件的得分。重新開始後，分數、剩餘時間、累積遊玩時間、等級、卡住狀態及落物都回到初始值。

**美術：** 寶寶是戴兔耳帽、髮量少、爬行姿勢的清晰像素風角色；月餅、炸彈、三種烤串與柚子各用獨立圖片，放大繪製時關閉平滑以保留清楚的像素邊緣。背景沿用目前的中秋場景與原有月餅切面。圖片載入前以背景色或表情符號作為暫時替代。

**直橫式：** 直式手機條件為視窗寬度不超過 700 CSS 像素且為直向，此時採 540 × 960 虛擬畫布與 `mooncake-portrait-fast.webp`，天空延伸以容納較高畫面。其他情況採 960 × 540 虛擬畫布與 `mooncake-fast.webp`。手機直式時開始／結算卡片疊於全視窗、物件說明改成單欄；橫式保留原本 16:9 畫面。畫布像素比例最高取裝置像素比 3，視窗改變尺寸或方向時更新配置。

## 6. 音效

遊戲使用瀏覽器 Web Audio API 即時合成音效，從按下開始或開啟音效的使用者手勢初始化。六種物件各有不同旋律或音色：月餅為雙音上行、炸彈為低音下墜、熟烤串為三音上行、焦烤串為方波下行、未熟烤串為三音下行、柚子為較高的三音上行。頂部按鈕可隨時開關音效，顯示 🔊／🔇；靜音狀態僅存在目前頁面記憶體中，不會寫入瀏覽器儲存空間。實際是否能播放也受瀏覽器音量與自動播放政策影響。

## 7. 技術與資料範圍

遊戲由單一 `index.html`（HTML、CSS、原生 JavaScript）和九張 WebP 圖片構成，不依賴帳號、遊戲伺服器、資料庫、第三方 JavaScript 套件或建置步驟。畫面以 Canvas 及 `requestAnimationFrame` 更新。分數、遊戲狀態只保留在目前分頁記憶體；重新整理後不會保留紀錄，也沒有排行榜或跨裝置同步。分數由瀏覽器端計算，不能作為有防作弊要求的競賽成績。

**部署所需檔案（全部放在網站根目錄）：**

```text
index.html
mooncake-fast.webp
mooncake-portrait-fast.webp
baby-pixel-fast.webp
mooncake-icon-fast.webp
bomb-icon-fast.webp
skewer-cooked-fast.webp
skewer-burnt-fast.webp
skewer-raw-fast.webp
pomelo-fast.webp
```

## 8. 可驗收情境

1. 在手機直式、手機橫式及電腦上開啟，分別看到對應比例背景；可直接拖動寶寶，沒有左右按鈕。
2. 開始後顯示 30 秒與 0 分；接月餅加 1 分、炸彈扣 2 分、柚子加 5 分，負分可以顯示。
3. 接熟烤串增加 3 秒；接焦烤串減少 5 秒且不出現負時間；時間歸零顯示結算。
4. 接未熟烤串後 3 秒無法移動，物件仍會落下；時間到後重新拖曳即可移動。
5. 存活滿 30 秒顯示「速度提升！」與 Lv.2，再過 10 秒進入 Lv.3；加秒不改變加速時點。
6. 六種物件圖案、浮動效果文字與音效各自對應；音效開關可切換。
7. 結算顯示「好飽喔~中秋節快樂!!」與大字得分；「再玩一次」從 30 秒、0 分、Lv.1 重新開始。

## 9. 發布為一般網頁：GitHub Pages

此專案是純靜態網頁，可將上述部署檔案上傳至 GitHub 儲存庫根目錄，使用 GitHub Pages 發布；訪客透過公開網址瀏覽，不需登入 GPT，也不需 GitHub 帳號。若使用 GitHub Free，GitHub Pages 所屬儲存庫須設為公開；因此原始碼與圖片也可被他人查看。儲存庫名稱若為 `baby-mid-autumn-game`，網址一般為 `https://<GitHub使用者名稱>.github.io/baby-mid-autumn-game/`。

操作步驟：建立公開儲存庫 → 把 `index.html` 與九張 WebP 放在儲存庫根目錄並提交 → 至 **Settings → Pages → Build and deployment** 選擇 **Deploy from a branch**，指定 `main` 與 `/(root)` → 儲存後等 GitHub Pages 完成發布 → 用無痕視窗實際開啟網址、測試手機直式與音效。只把檔案放在 GitHub 儲存庫不會自動變成 Pages 網站，必須設定發布來源。未來更新原有 Sites 網址和 GitHub Pages 網址時，兩邊的內容也需各自同步。

目前的 Sites 遊戲網址也已設定為公開存取；若只是分享遊戲，可先用無痕視窗驗證現有網址能否直接開啟。GitHub Pages 是另外一個獨立的公開託管選項。

**GitHub 官方參考：** [GitHub Pages 簡介](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)、[建立 GitHub Pages 網站](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)、[設定發布來源](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)。

## 10. 開發踩坑紀錄

圖片傳輸與首屏載入、Canvas 舊版瀏覽器 fallback、iOS Web Audio 恢復，以及實機測試範圍，另整理於 [LESSONS_LEARNED.md](LESSONS_LEARNED.md)。新專案可沿用該文件的檢查清單與故障紀錄格式。
