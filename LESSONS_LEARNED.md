# 網頁遊戲踩坑紀錄：圖片載入與 iOS 相容性

**紀錄日期：** 2026-09-25  
**適用範圍：** 本專案，以及使用 Canvas、圖片素材、觸控與 Web Audio API 的手機網頁遊戲  
**關聯文件：** [遊戲設計與實作規則](GAME_DESIGN.md)

本文把實際觀察到的現象、已採取的修正、驗證範圍和仍待實機確認的項目分開記錄。其他專案遇到類似問題時，先從相同檢查點開始，再以目標瀏覽器的實際錯誤訊息確認根因。

## 事件一：開始頁與遊戲圖片載入偏慢

### 使用者觀察到的現象

- 開始頁的物件圖示和寶寶圖需要等候才顯示。
- 進入遊戲後，寶寶與背景也感覺載入緩慢。
- 問題在實際網頁使用時明顯；單純檢查畫面或本機開啟，不一定重現相同等待時間。

### 原因分析

圖片顯示前至少要經過網路傳輸（若尚未快取）、圖片解碼，再由 Canvas 繪製。大型原始圖、首次載入多個素材、慢速行動網路和裝置解碼速度都會增加等待。這次專案採用多張獨立素材，涵蓋首頁寶寶圖、六種物件圖，以及直式與橫式背景；因此不能只看單張圖片大小，要同時檢查整頁首次下載量與解碼成本。

目前版本把遊戲素材整理為專案目錄內的 WebP 檔，使用相對路徑，並以較適合網頁傳輸的圖片取代較重的原始素材。首頁與遊戲共用的圖在程式初始化時建立 `Image` 並設定 `src`，載入完成時重繪畫面；素材未就緒時則繪製背景色或表情符號作為暫時替代。這樣做可避免圖片載入期間整個 Canvas 空白，但不代表慢速網路上所有素材都已載完才允許開始遊戲。

### 本版本素材現況

下表為目前 GitHub 專案中檔案的實際大小與像素尺寸。尺寸使用 `identify` 讀取；檔案大小為本次紀錄時的位元組數。這裡沒有保留轉檔前原始檔案與其大小，因此不記錄虛構的壓縮比例或載入秒數。

| 素材 | 尺寸 | 大小 |
| --- | ---: | ---: |
| `baby-pixel-fast.webp` | 512 × 414 | 162,168 B |
| `mooncake-icon-fast.webp` | 320 × 320 | 108,036 B |
| `bomb-icon-fast.webp` | 320 × 320 | 43,832 B |
| `skewer-cooked-fast.webp` | 320 × 320 | 61,792 B |
| `skewer-burnt-fast.webp` | 320 × 320 | 48,364 B |
| `skewer-raw-fast.webp` | 320 × 320 | 51,018 B |
| `pomelo-fast.webp` | 320 × 320 | 95,002 B |
| `mooncake-fast.webp`（橫式背景） | 1672 × 941 | 215,160 B |
| `mooncake-portrait-fast.webp`（直式背景） | 941 × 1672 | 202,802 B |
| `line-share-v2.jpg`（分享縮圖） | 1200 × 630 | 306,632 B |

九張遊戲 WebP 合計 **988,174 B（約 965 KiB）**；分享縮圖另計。這是檔案總量，不等於每次開始遊戲都會下載相同的位元組數：瀏覽器快取、使用的方向、載入策略及服務端快取都會影響實際傳輸量。

### 本次處理與可重用作法

1. 將遊戲內圖片放在專案中，以適合內容的 WebP 圖片取代較重的原始素材；像素風角色也保留足夠像素尺寸，避免為了縮小檔案而把畫面弄糊。
2. 將背景依直式、橫式分開，避免用不合比例的大圖裁切所有畫面；圖示則以遊戲顯示需求設定尺寸。
3. 使用清楚且穩定的相對路徑，並在部署清單中檢查所有圖片是否一併上傳。檔名與大小寫必須完全相同，特別是 Linux 靜態託管環境。
4. 圖片尚未載入時先繪製安全的背景與替代圖形；`drawImage` 前檢查圖片已完成載入且 `naturalWidth > 0`，避免載入失敗造成錯誤或空白物件。
5. 量測整頁首屏所需資產，而非只優化首頁小圖。首頁立即可見的素材可優先載入；遊戲後段才需要的大圖可考慮延後載入。若採延後載入，開始遊戲前須有載入狀態或可靠的即時 fallback。
6. 對像素美術可在 Canvas 繪製時關閉 `imageSmoothingEnabled`，讓放大後的像素邊緣保持清楚；調整圖片品質後，需在實際遊戲縮放比例下目視確認。
7. 用瀏覽器 Network 面板按「Disable cache」測冷快取，再以一般快取測第二次載入；記錄下載大小、圖片請求、載入時間和手機網路下的體感。只在本機或快取已暖的分頁測試會低估首次載入時間。

### 已知限制

目前紀錄有優化後檔案大小，但沒有原始素材大小、冷快取基準時間或不同網速的量測結果。因此只能確認目前素材為本地 WebP 且總量約 965 KiB，不能聲稱已降低特定百分比或保證特定手機上的載入秒數。新專案應在優化前後保存同一測試條件下的數據。

## 事件二：舊 iPhone 上遊戲物件消失，iPad 沒有音效

### 使用者觀察到的現象

- 使用者回報 iPad 上沒有音效。
- 舊款 iPhone 7 上看不到寶寶和掉落物。
- 桌面瀏覽器以開發者工具切換 iPad 或 iPhone 16 的裝置尺寸時正常。
- 開發者工具只模擬視窗大小、像素比例及部分輸入條件；它仍執行桌面瀏覽器的 JavaScript 與圖形實作，不能代表舊版 iOS Safari。

### 寶寶與掉落物：已修正的相容性風險

本遊戲在繪製掉落物卡片時呼叫 `CanvasRenderingContext2D.roundRect()`。這個 Canvas API 在較新的瀏覽器才可用；較舊 iOS Safari 不一定提供。若呼叫不存在的函式會拋出 JavaScript 錯誤，而遊戲主迴圈每一影格都要繪製掉落物卡片，錯誤可能中斷該影格繪製，使寶寶或落物看似消失。

這是依照程式呼叫點與使用者裝置症狀作出的**高度合理根因推測**；手上沒有 iPhone 7 的 Safari console log 或實機錄影，故不能標註為已在該手機確認的錯誤堆疊。實際 iPhone 7 可支援的最高 iOS 版本也會因設備／系統狀態而不同；排查時應讀取實機 iOS 與 Safari 版本，不應只用機型猜 API 支援狀態。

**修正：** 目前 `rounded()` 先以 `typeof ctx.roundRect === 'function'` 做能力偵測；支援時使用原生 API，不支援時以 `moveTo`、`arcTo` 和 `closePath` 畫出圓角路徑。這個 fallback 不依賴新版 Canvas API。錯誤處理的原則是先偵測功能，不要只依賴 User-Agent 字串推斷支援度。

### iOS 音效：加入恢復處理後的實機結果

Safari/iOS 對 Web Audio 有使用者互動與音訊工作階段限制。音訊內容可能受靜音開關、音量、瀏覽器自動播放政策、分頁狀態，以及 AudioContext 被暫停或中斷影響。桌面開發者工具無法完整模擬 iPad 的音訊路由和系統限制。

**修正：** `ensureAudio()` 在音效未靜音時建立或恢復 `AudioContext`；只要狀態不是 `running`（包含 `suspended` 或 `interrupted`），就嘗試呼叫 `resume()`，並處理非同步拒絕以免出現未處理 Promise rejection。開始遊戲與畫布 `pointerdown` 都會呼叫它，讓恢復操作與實際使用者手勢相連。靜音時不主動恢復音訊。

這項修改增加了恢復機會，但不等於所有 iOS 裝置都已驗證成功。最新實機回報（2026-09-26）：iPad 有音效；iPhone 7（iOS 15.8.8）以 Safari 和 Chrome 開啟，且寶寶接到物件時仍無音效。兩個瀏覽器都無聲，表示目前不能把原因歸咎於單一瀏覽器；需再分辨裝置音量／靜音狀態、音訊輸出路由、舊版 iOS 的 Web Audio 行為，或 `resume()` 未成功等可能性。

**先確認遊戲的觸發條件：**目前只在寶寶接到物件時呼叫 `playCatch()`，首頁、倒數 3-2-1 和單純開始遊戲都不會播放音效。因此測試時要確認音效圖示為開啟，開始遊戲後確實接到至少一個掉落物。

**下一輪排查順序：**

1. iPhone 7 播放其他網頁影片或音樂，確認手機喇叭與媒體音量正常；遊戲進行中按音量增加，並暫時關閉藍牙／拔除耳機以排除音訊輸出路由。
2. 確認 iPhone 側邊響鈴／靜音開關不是靜音位置。Apple 的喇叭排查也建議先關閉靜音模式並檢查音量；這只能排除裝置設定，不能證明遊戲音效程式正常（[Apple 支援：iPhone、iPad 或 iPod touch 喇叭無聲或聲音失真](https://support.apple.com/en-bn/118432)）。
3. 若其他媒體有聲、且已確實接到物件仍無聲，記錄 `AudioContext.state`；在 `resume()` 失敗時保留錯誤訊息，並加一個使用者手勢觸發的「測試音效」診斷入口。再用實機 Safari Web Inspector 查 console，避免只靠開發者工具裝置模擬判斷。

**相容性線索與修正：**原本 `ensureAudio()` 只從 `click` 和 `pointerdown` 嘗試恢復 AudioContext。WebKit 過往的 iOS 相容性討論指出，部分 iOS Safari 情境要求在 `touchend` 使用者手勢中啟動 Web Audio（[WebKit Bug 177292](https://bugs.webkit.org/show_bug.cgi?id=177292)）。這和 iOS 15.8.8 的失敗現象相符，但仍是根因假設，尚無 iPhone 7 上的 AudioContext 狀態或 console 證據。

已在程式加入觸控結束時的恢復嘗試，掛在開始按鈕、音效按鈕和遊戲畫布上；這讓舊版 Safari 有符合 `touchend` 時機的解鎖機會。這是針對相容性線索的修正，**尚未由該 iPhone 7 回歸驗證**。靜音／音量、輸出裝置，以及該 iPhone 7 的其他網頁媒體是否有聲仍待確認。不要把「呼叫過 `resume()`」當作音效已恢復的驗收結果。

### 驗證結果與範圍

- 已檢查 JavaScript 語法。
- 使用模擬 Canvas context 驗證不含 `roundRect` 時的 `arcTo` fallback 可完成圓角路徑繪製。
- 已在雲端桌面瀏覽器實際開始遊戲，觀察到寶寶與掉落物出現；也檢查部署頁面包含相容性 fallback。
- 使用者回報：iPad 實機有音效；iPhone 7（iOS 15.8.8）在 Safari 與 Chrome 中接到物件仍無音效。已加入 `touchend` AudioContext 恢復路徑並通過 JavaScript 語法檢查；尚未在該手機回歸測試，根因仍待驗證。

## 下一個手機網頁專案的檢查清單

### 圖片與載入

- [ ] 列出每張圖片的來源、格式、像素尺寸、位元組數，以及使用畫面。
- [ ] 計算首屏必要素材總量；避免首頁和遊戲開始前下載未使用的大圖。
- [ ] 對照片或複雜插畫評估 WebP/AVIF；對像素圖或透明圖實測品質、透明邊緣與瀏覽器支援。
- [ ] 保留合理的顯示尺寸；壓縮後在真實 CSS/Canvas 尺寸下檢查清晰度。
- [ ] 圖片未載入、404、格式不支援時仍能顯示可操作畫面，並留下可排查錯誤資訊。
- [ ] 確認部署路徑、檔名字母大小寫與相對 URL；不要只在本機確認圖片可讀。
- [ ] 冷快取和暖快取都測一次，並記錄網路與裝置條件。

### Canvas 與瀏覽器 API

- [ ] 搜尋所有非基礎或較新的 API（Canvas、音訊、觸控、全螢幕等），逐項做功能偵測或提供 fallback。
- [ ] 最舊支援瀏覽器缺少某功能時，畫面是否會有未捕捉錯誤並中止整個 `requestAnimationFrame` 迴圈？
- [ ] 在動畫入口或繪製各物件處理錯誤，避免單一素材或效果失敗令整個畫面消失。
- [ ] 改變方向、縮放、devicePixelRatio 與畫布尺寸後，重新檢查座標和繪製狀態。

### iOS 觸控、音訊與實機測試

- [ ] 測試最低支援版本的真實 iPhone/iPad Safari；記錄型號、iOS 版本與 Safari 版本。
- [ ] 保留 Safari Web Inspector 的 console、network log 或錄影；開發者工具裝置模擬只當版面測試。
- [ ] 在有效的點擊／觸控事件內建立或恢復 `AudioContext`，並處理 `resume()` Promise 失敗。
- [ ] 測試 `AudioContext.state` 不為 `running` 的恢復路徑、分頁返回前景、音效開關及靜音狀態。
- [ ] 實際聽取每種聲音；確認系統靜音、低音量或輸出裝置切換時的預期提示與行為。
- [ ] 確認 Pointer Events、`setPointerCapture`、方向切換與視窗失焦後，拖曳能正常開始、移動及結束。

## 可複製的故障紀錄格式

遇到新問題時，新增一節並填妥下列欄位。把推測和證據分開，方便下個案例直接套用，也避免把偶然修好誤當成已找到根因。

```text
日期與版本：
裝置／作業系統／瀏覽器：
重現步驟：
預期結果：
實際結果：
冷快取／暖快取及網路條件：
console／network 證據：
根因：已確認／待驗證（依據）：
修改內容：
回歸測試與結果：
尚未覆蓋的裝置或情境：
```
